<df> %>%
  summarise(
    count = n(),
    mean = mean(<var>),
    sd = sd(<var>),
    median = median(<var>),
    IQR = IQR(<var>),
    min = min(<var>),
    max = max(<var>),
    range = range(<var>)
            )